<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Server-side Validation</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h2>User Registration</h2>

    <% if (errors.length > 0) { %>
        <ul class="errors">
            <% errors.forEach(e => { %>
                <li><%= e.msg %></li>
            <% }) %>
        </ul>
    <% } %>

    <form action="/submit" method="POST">
        <label>Username:</label>
        <input type="text" name="username" value="<%= inputData.username || '' %>"><br>

        <label>Email:</label>
        <input type="text" name="email" value="<%= inputData.email || '' %>"><br>

        <label>Password:</label>
        <input type="password" name="password"><br>

        <button type="submit">Register</button>
    </form>
</body>
</html>
